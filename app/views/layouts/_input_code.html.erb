<% if current_user.present? && current_user.team.present? %>
  <%= form_for :code_string, url: root_path, html: {class: 'button_to'} do |f| %>
		<%= f.label :code, 'Введите код:' %>
		<%= f.text_field :code %>
		<%= f.submit 'OK' %>
    <%= link_to 'Мобильная версия', m_path %>
  <% end %>
<% end %>