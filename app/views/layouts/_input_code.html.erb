<% if current_user.present? && current_user.team.present? %>
  <%= form_for :code_string, html: {class: 'button_to'} do |f| %>
		<%= f.label :data, 'Введите код:' %>
		<%= f.text_field :code %>
		<%= f.submit 'OK' %>
  <% end %>
<% end %>