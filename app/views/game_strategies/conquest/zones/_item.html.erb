<div class='zone openable_holder'>
  <% if zone.present? %>
    <div class='zone_header'>
      <%= openable_tag(zone.make_id) %>
      <%= content_tag(:span, zone.name, class: 'caption') %>
    </div>
  	<div class='openable'>
  	  <% zone.tasks.unincluded.by_order.each do |task| %>
  		  <%= render 'game_strategies/conquest/tasks/item', task: task  %>
  	  <% end %>
    </div>
  <% else %>
    <div class='zone_header'>
      <%= openable_tag('free_zone0') %>
      <%= content_tag(:span, 'Свободные задания', class: 'caption') %>
    </div>
    <div class='openable'>
      <% @game.tasks.unincluded.unzoned.by_order.each do |task| %>
        <%= render 'game_strategies/conquest/tasks/item', task: task %>
      <% end %>
    </div>
	<% end %>
</div>