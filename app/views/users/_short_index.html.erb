<div class="separated">
  <span class="caption">Игроки: </span><br>
  <div class="user_link_holder">
  <% users.each do |user|%>
      <%= link_to_user(user, can?(:exclude, user)) %>
  <% end %>
  </div>
</div>
